import 'package:flutter/material.dart';

class ReadUserPage extends StatelessWidget {
  const ReadUserPage({ Key? key }) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      //TODO: Caso eu precise de ler um usuario
    );
  }
}